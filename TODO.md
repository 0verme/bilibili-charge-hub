# TODO

## 非紧急

- [ ] 每日任务视频源增加“特别关注”优先级（当前为关注列表 → 排行榜）。
- [ ] 每日任务支持按账号查看历史执行趋势（投币数/分享/观看统计）。
- [x] 增加“通知对账任务”，在 NAS、Docker 或数据库中断恢复后自动发现并修复静默遗漏的通知。
  - 每小时自动扫描最近 24 小时入库的充电记录，校验每条记录是否存在对应的 `new_charge` outbox 事件。
  - 对缺失事件使用与正常采集完全一致的稳定去重键和 payload 补建。
  - 校验已订阅渠道是否存在投递记录，缺失时补建并交回现有投递链路；已成功投递永不重发，失败通知尊重退避与重试预算。
  - 任务幂等执行，严格按用户隔离，不调用 Bilibili API；每次执行输出结构化审计摘要（扫描/补建/补齐投递/重入队/错误数）。
  - 管理员可通过通知中心手动触发；默认 lookback 24 小时、单次扫描上限 2000 条。
  - 验收覆盖：记录已入库但事件缺失、事件存在但未投递、已成功投递、任务重复执行四类场景（另含租户隔离、扫描窗口、扫描上限与无 Bilibili 调用）。
